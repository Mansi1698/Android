<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mansi.firstapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Calculator" />
        <activity android:name=".Layout_check" />
        <activity android:name=".EmployeeDetails" />
        <activity android:name=".ListViewActivity">
            y
        </activity>
        <activity android:name=".AutoCompleteView" />
        <activity android:name=".MultiAutoComplete" />
        <activity android:name=".menu"></activity>
        <activity android:name=".dialog">

        </activity>
        <activity android:name=".fragment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>